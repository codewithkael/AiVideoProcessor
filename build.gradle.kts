plugins {
    // Root project: keep empty for now; module-specific plugins are in ai-video-processor/build.gradle.kts
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
